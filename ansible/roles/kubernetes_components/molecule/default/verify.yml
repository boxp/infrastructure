---
- name: Verify
  hosts: all
  gather_facts: true
  vars:
    journald_persistent_storage: true
    # Must mirror converge.yml — verify.yml is a separate play and does not see role defaults.
    node_resilience_watchdog: true
    node_resilience_hung_task_panic: true
    node_resilience_runtime_watchdog_sec: "15"
    node_resilience_reboot_watchdog_sec: "15"
  tasks:
    - name: Check if CRI-O is installed
      ansible.builtin.package_facts:
        manager: auto

    - name: Assert CRI-O packages are installed
      ansible.builtin.assert:
        that:
          - "'cri-o' in ansible_facts.packages"
          - "'cri-tools' in ansible_facts.packages"
        fail_msg: "CRI-O packages are not installed"

    - name: Check if Kubernetes packages are installed
      ansible.builtin.assert:
        that:
          - "'kubelet' in ansible_facts.packages"
          - "'kubeadm' in ansible_facts.packages"
          - "'kubectl' in ansible_facts.packages"
        fail_msg: "Kubernetes packages are not installed"

    - name: Check if CRI-O configuration exists
      ansible.builtin.stat:
        path: /etc/crio/crio.conf.d/02-cgroup-manager.conf
      register: crio_config

    - name: Assert CRI-O configuration exists
      ansible.builtin.assert:
        that:
          - crio_config.stat.exists
        fail_msg: "CRI-O configuration file does not exist"

    - name: Check if CRI-O log rotation configuration exists
      ansible.builtin.stat:
        path: /etc/crio/crio.conf.d/99-log-rotation.conf
      register: crio_log_rotation_config

    - name: Assert CRI-O log rotation configuration exists
      ansible.builtin.assert:
        that:
          - crio_log_rotation_config.stat.exists
        fail_msg: "CRI-O log rotation configuration file does not exist"

    - name: Read CRI-O log rotation configuration
      ansible.builtin.slurp:
        path: /etc/crio/crio.conf.d/99-log-rotation.conf
      register: crio_log_rotation_content

    - name: Assert CRI-O log size limit is configured
      ansible.builtin.assert:
        that:
          - "'log_size_max = 1048576' in (crio_log_rotation_content.content | b64decode)"
        fail_msg: "CRI-O log size limit is not configured"

    - name: Check if kubelet configuration exists
      ansible.builtin.stat:
        path: /etc/kubernetes/kubelet-config.yaml
      register: kubelet_config

    - name: Assert kubelet configuration exists
      ansible.builtin.assert:
        that:
          - kubelet_config.stat.exists
        fail_msg: "Kubelet configuration file does not exist"

    - name: Read kubelet configuration
      ansible.builtin.slurp:
        path: /etc/kubernetes/kubelet-config.yaml
      register: kubelet_config_content

    - name: Assert kubelet container log rotation is configured
      ansible.builtin.assert:
        that:
          - "'containerLogMaxSize: 1Mi' in (kubelet_config_content.content | b64decode)"
          - "'containerLogMaxFiles: 2' in (kubelet_config_content.content | b64decode)"
        fail_msg: "Kubelet container log rotation is not configured"

    - name: Check if kubelet systemd configuration exists
      ansible.builtin.stat:
        path: /etc/systemd/system/kubelet.service.d/10-kubeadm.conf
      register: kubelet_systemd

    - name: Assert kubelet systemd configuration exists
      ansible.builtin.assert:
        that:
          - kubelet_systemd.stat.exists
        fail_msg: "Kubelet systemd configuration does not exist"

    - name: Check if sysctl configuration exists
      ansible.builtin.stat:
        path: /etc/sysctl.d/k8s.conf
      register: sysctl_config

    - name: Assert sysctl configuration exists
      ansible.builtin.assert:
        that:
          - sysctl_config.stat.exists
        fail_msg: "Kubernetes sysctl configuration does not exist"

    - name: Check if kernel modules configuration exists
      ansible.builtin.stat:
        path: /etc/modules-load.d/k8s.conf
      register: modules_config

    - name: Check CRI-O service status
      ansible.builtin.systemd:
        name: crio
      register: crio_service
      failed_when: false

    - name: Check kubelet service status
      ansible.builtin.systemd:
        name: kubelet
      register: kubelet_service
      failed_when: false

    - name: Verify CRI-O socket exists
      ansible.builtin.stat:
        path: /var/run/crio/crio.sock
      register: crio_socket
      failed_when: false

    - name: Check kubeadm version
      ansible.builtin.command: kubeadm version -o short
      register: kubeadm_version
      changed_when: false

    - name: Assert kubeadm version is correct
      ansible.builtin.assert:
        that:
          - "'v1.32' in kubeadm_version.stdout"
        fail_msg: "Kubeadm version is not as expected"

    - name: Check kubectl version
      ansible.builtin.command: kubectl version --client -o yaml
      register: kubectl_version
      changed_when: false

    - name: Assert kubectl version is correct
      ansible.builtin.assert:
        that:
          - "'1.32' in kubectl_version.stdout"
        fail_msg: "Kubectl version is not as expected"

    - name: Check if persistent journal directory exists
      ansible.builtin.stat:
        path: /var/log/journal
      register: journal_dir

    - name: Assert persistent journal directory exists
      ansible.builtin.assert:
        that:
          - journal_dir.stat.exists
          - journal_dir.stat.isdir
        fail_msg: "/var/log/journal directory does not exist — journald persistent storage not configured"
      when: journald_persistent_storage | bool

    - name: Check if journald drop-in config exists
      ansible.builtin.stat:
        path: /etc/systemd/journald.conf.d/10-persistent-storage.conf
      register: journald_dropin

    - name: Assert journald drop-in config is absent when persistent storage is disabled
      ansible.builtin.assert:
        that:
          - not journald_dropin.stat.exists
        fail_msg: "journald persistent-storage drop-in must be absent when persistent storage is disabled"
      when: not (journald_persistent_storage | bool)

    - name: Assert journald drop-in config exists when persistent storage is enabled
      ansible.builtin.assert:
        that:
          - journald_dropin.stat.exists
        fail_msg: "journald drop-in /etc/systemd/journald.conf.d/10-persistent-storage.conf does not exist"
      when: journald_persistent_storage | bool

    - name: Read journald drop-in config
      ansible.builtin.slurp:
        path: /etc/systemd/journald.conf.d/10-persistent-storage.conf
      register: journald_dropin_content
      when: journald_persistent_storage | bool

    - name: Assert journald is configured for persistent storage
      ansible.builtin.assert:
        that:
          - "'Storage=persistent' in (journald_dropin_content.content | b64decode)"
          - "'SystemMaxUse=512M' in (journald_dropin_content.content | b64decode)"
          - "'RuntimeMaxUse=256M' in (journald_dropin_content.content | b64decode)"
          - "'MaxRetentionSec=1month' in (journald_dropin_content.content | b64decode)"
        fail_msg: "journald drop-in is missing Storage=persistent, SystemMaxUse=512M, RuntimeMaxUse=256M, or MaxRetentionSec=1month"
      when: journald_persistent_storage | bool

    - name: Read rsyslog logrotate config
      ansible.builtin.slurp:
        path: /etc/logrotate.d/rsyslog
      register: rsyslog_logrotate_content

    - name: Assert rsyslog logrotate has maxsize limit configured
      ansible.builtin.assert:
        that:
          - "'maxsize 200M' in (rsyslog_logrotate_content.content | b64decode)"
        fail_msg: "rsyslog logrotate config is missing maxsize 200M — disk exhaustion guard not in place"

    - name: Check if logrotate-rsyslog-hourly timer unit exists
      ansible.builtin.stat:
        path: /etc/systemd/system/logrotate-rsyslog-hourly.timer
      register: rsyslog_hourly_timer

    - name: Assert hourly logrotate timer is deployed for rsyslog
      ansible.builtin.assert:
        that:
          - rsyslog_hourly_timer.stat.exists
        fail_msg: "logrotate-rsyslog-hourly.timer is missing — size limit only takes effect at daily logrotate without it"

    - name: Check if systemd watchdog drop-in exists
      ansible.builtin.stat:
        path: /etc/systemd/system.conf.d/10-watchdog.conf
      register: watchdog_dropin

    - name: Assert systemd watchdog drop-in exists when enabled
      ansible.builtin.assert:
        that:
          - watchdog_dropin.stat.exists
        fail_msg: >-
          /etc/systemd/system.conf.d/10-watchdog.conf does not exist —
          a hung node would stay unreachable instead of resetting itself
      when: node_resilience_watchdog | bool

    - name: Assert systemd watchdog drop-in is absent when disabled
      ansible.builtin.assert:
        that:
          - not watchdog_dropin.stat.exists
        fail_msg: "watchdog drop-in must be absent when node_resilience_watchdog is false"
      when: not (node_resilience_watchdog | bool)

    - name: Read systemd watchdog drop-in
      ansible.builtin.slurp:
        path: /etc/systemd/system.conf.d/10-watchdog.conf
      register: watchdog_dropin_content
      when: node_resilience_watchdog | bool

    - name: Assert watchdog timeouts are configured
      ansible.builtin.assert:
        that:
          - >-
            'RuntimeWatchdogSec=' ~ node_resilience_runtime_watchdog_sec
            in (watchdog_dropin_content.content | b64decode)
          - >-
            'RebootWatchdogSec=' ~ node_resilience_reboot_watchdog_sec
            in (watchdog_dropin_content.content | b64decode)
          # sunxi-wdt (H618) の max_timeout は 16 秒。超えると systemd が watchdog を無効化する。
          - node_resilience_runtime_watchdog_sec | int > 0
          - node_resilience_runtime_watchdog_sec | int <= 16
          - node_resilience_reboot_watchdog_sec | int > 0
          - node_resilience_reboot_watchdog_sec | int <= 16
        fail_msg: >-
          watchdog drop-in must carry the declared timeouts, and both must stay within
          the 16s max_timeout of sunxi-wdt (otherwise systemd disables the watchdog)
      when: node_resilience_watchdog | bool

    - name: Check if hung task sysctl drop-in exists
      ansible.builtin.stat:
        path: /etc/sysctl.d/90-hung-task.conf
      register: hung_task_dropin

    - name: Assert hung task sysctl drop-in exists when enabled
      ansible.builtin.assert:
        that:
          - hung_task_dropin.stat.exists
        fail_msg: "/etc/sysctl.d/90-hung-task.conf does not exist"
      when: node_resilience_hung_task_panic | bool

    - name: Assert hung task sysctl drop-in is absent when disabled
      ansible.builtin.assert:
        that:
          - not hung_task_dropin.stat.exists
        fail_msg: "hung task sysctl drop-in must be absent when node_resilience_hung_task_panic is false"
      when: not (node_resilience_hung_task_panic | bool)

    - name: Read hung task sysctl drop-in
      ansible.builtin.slurp:
        path: /etc/sysctl.d/90-hung-task.conf
      register: hung_task_dropin_content
      when: node_resilience_hung_task_panic | bool

    - name: Assert hung task panic is configured with a non-zero reboot timer
      ansible.builtin.assert:
        that:
          - "'kernel.hung_task_panic = 1' in (hung_task_dropin_content.content | b64decode)"
          - "'kernel.hung_task_timeout_secs = 300' in (hung_task_dropin_content.content | b64decode)"
          - "'kernel.panic = 10' in (hung_task_dropin_content.content | b64decode)"
        fail_msg: >-
          hung task drop-in must set hung_task_panic=1 together with a non-zero kernel.panic,
          otherwise the panic never reboots the board
      when: node_resilience_hung_task_panic | bool

    # Watchdog device check: in Docker the device is absent, so the assert task in
    # node_resilience.yml is skipped (guarded by ansible_virtualization_type != "docker").
    # Verify the drop-in was written correctly — the device-existence assertion is not
    # exercised here because it only runs on real hardware.
    - name: Confirm watchdog drop-in was written (device-check skip is expected in Docker)
      ansible.builtin.assert:
        that:
          - watchdog_dropin.stat.exists
        fail_msg: >-
          watchdog drop-in was not written even though node_resilience_watchdog is true
      when: node_resilience_watchdog | bool
