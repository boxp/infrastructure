# IS01 (armv7) 向けにビルドする codex のバージョン。
#
# Renovate が openai/codex の GitHub Release を追って自動で更新する。
# 更新 PR がマージされると build-codex-armv7.yml が発火してビルドされる。
#
# 🚨 alpha は追わない。頻繁に出るうえ、実機に載せるには安定版で十分なため。
#    renovate.json5 の customManagers で ^rust-v(\d+\.\d+\.\d+)$ に限定している。
CODEX_VERSION=0.154.0
